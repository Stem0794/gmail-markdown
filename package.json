{
  "name": "gmail-markdown-extension",
  "version": "1.3.0",
  "description": "Gmail Markdown Chrome extension",
  "private": true,
  "scripts": {
    "test": "mocha",
    "test:e2e": "playwright test",
    "build:zip": "zip -r extension.zip . -x '*.git*' 'test/*' 'node_modules/*' 'extension.zip' '.npmignore' '*playwright*' 'package-lock.json' 'package.json'"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "chai": "^4.3.7",
    "jsdom": "^22.1.0",
    "marked": "^9.1.2",
    "mocha": "^10.2.0",
    "turndown": "^7.1.1"
  }
}
